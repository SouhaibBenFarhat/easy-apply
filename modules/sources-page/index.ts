// L3 — the Sources page feature (PLAN.md §6 page 3): one card per provider —
// toggle, last sync state, key management for keyed sources, attribution.
export { SourcesPage } from './components/SourcesPage'
