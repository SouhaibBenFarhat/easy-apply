name: Release Build (macOS dmg)

# Runs when a GitHub Release is published (i.e. you publish the Release Drafter
# draft). Builds the app on a macOS runner, attaches the dmg to that release via
# electron-builder's GitHub publisher, then bumps version + sha256 in the
# homebrew-tap cask so `brew upgrade` picks it up — no manual steps after
# clicking Publish.

on:
  release:
    types: [published]

permissions:
  contents: write # upload the dmg to the release

jobs:
  build-and-publish:
    name: Build, attach dmg & bump tap
    runs-on: macos-latest
    env:
      TAP_TOKEN: ${{ secrets.TAP_GITHUB_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Derive version from tag
        id: version
        # Tag is like "v0.2.0"; the app bundle wants the bare "0.2.0".
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Build renderer/main bundles
        run: |
          pnpm generate:theme
          pnpm exec electron-vite build

      - name: Package dmg and attach to the release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Unsigned build — the cask postflight strips the quarantine flag.
          CSC_IDENTITY_AUTO_DISCOVERY: 'false'
        run: |
          pnpm exec electron-builder --mac --arm64 --publish always \
            -c.extraMetadata.version=${{ steps.version.outputs.version }}

      - name: Bump cask in homebrew-tap
        if: env.TAP_TOKEN != ''
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          SHA=$(shasum -a 256 dist/EasyApply-arm64.dmg | cut -d' ' -f1)

          git clone --depth 1 --branch master \
            "https://x-access-token:${TAP_TOKEN}@github.com/SouhaibBenFarhat/homebrew-tap.git" tap
          cd tap
          sed -i '' -E "s|^  version \".*\"|  version \"${VERSION}\"|" Casks/easyapply.rb
          sed -i '' -E "s|^  sha256 \".*\"|  sha256 \"${SHA}\"|" Casks/easyapply.rb

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -am "easyapply ${VERSION}"
          git push origin master
